@model CarCare.Models.WarrantyViewModel

<script src="~/Scripts/jquery-1.10.2.min.js"></script>
<link href="~/Content/bootstrap.css" rel="stylesheet" />

@using (@Html.BeginForm("SaveWarranty", "Warranty", FormMethod.Post))
{
    <div class="container">
        <div class="row">
            <table class="table">
                <tr>
                    <td>
                        Policy Number
                    </td>
                    <td>
                        @Html.TextBoxFor(m => m.PolicyNumber)
                    </td>
                </tr>
                <tr>
                    <td>
                        Vehicle (VIN)
                    </td>
                    <td>
                        @Html.DropDownListFor(m => m.VehicleId, ((List<SelectListItem>)ViewBag.Vehicles).Select(m => new SelectListItem { Text = m.Text, Value = m.Value }))
                    </td>
                </tr>
                <tr>
                    <td>
                        Warranty Provider
                    </td>
                    <td>
                        @Html.TextBoxFor(m => m.WarrantyProvider)
                    </td>
                </tr>
                <tr>
                    <td>
                        Cost
                    </td>
                    <td>
                        @Html.TextBoxFor(m => m.WarrantyCost)
                    </td>
                </tr>
                <tr>
                    <td>
                        Start Date
                    </td>
                    <td>
                        @Html.TextBoxFor(model => model.WarrantyStartDate,
                            new { @type = "date", @Value = Model.WarrantyStartDate.ToString("yyyy-MM-dd") })
                    </td>
                </tr>
                <tr>
                    <td>
                        Expiration Date
                    </td>
                    <td>
                        @Html.TextBoxFor(model => model.WarrantyExpirationDate,
                            new { @type = "date", @Value = Model.WarrantyExpirationDate.ToString() })
                    </td>
                </tr>
                <tr>
                    <td>
                        Coverage
                    </td>
                    <td>
                        @Html.TextBoxFor(m => m.WarrantyCoverage)
                    </td>
                </tr>
                <tr>
                    <td>
                        <button class="btn btn-primary" type="submit">Save</button>
                        @Html.HiddenFor(m => m.WarrantyId)
                    </td>
                    <td>
                        <button type="button" class="btn btn-info" data-dismiss="modal">Cancel</button>
                    </td>
                </tr>
            </table>

        </div>

    </div>
}